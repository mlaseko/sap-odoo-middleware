@page "/documents"
@model SapOdooMiddleware.Pages.Documents.IndexModel

<div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:16px;">
    <h2 style="margin:0;">Recent documents</h2>
    <a class="btn" href="/documents/upload">Upload new</a>
</div>

@if (TempData["Notice"] is string notice)
{
    <div class="notice">@notice</div>
}

@if (Model.Docs.Count == 0)
{
    <p>No documents uploaded yet.</p>
}
else
{
    <table>
        <thead>
            <tr>
                <th>Uploaded</th>
                <th>File</th>
                <th>Invoice #</th>
                <th>Invoice date</th>
                <th>Supplier</th>
                <th class="num">Pages</th>
                <th>Status</th>
                <th>Validation</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        @foreach (var d in Model.Docs)
        {
            <tr>
                <td>@d.UploadedAt.ToLocalTime().ToString("yyyy-MM-dd HH:mm")</td>
                <td>@d.OriginalFilename</td>
                <td>@d.InvoiceNumber</td>
                <td>@(d.InvoiceDate?.ToString("yyyy-MM-dd"))</td>
                <td>@d.Supplier</td>
                <td class="num">@d.PageCount</td>
                <td>@d.Status</td>
                <td>@d.ValidationStatus</td>
                <td><a href="/documents/@d.Id">View</a></td>
            </tr>
        }
        </tbody>
    </table>
}
